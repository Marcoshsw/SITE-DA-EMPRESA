services:
  site-grupo-mano:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      GIN_MODE: release
      FRONTEND_DIR: /app/frontend
      CORS_ORIGINS: "*"
    restart: unless-stopped